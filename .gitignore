# Bazel
/bazel-*
*.bzl~

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
/vendor/
/go.sum # Often committed, but can be gitignored if strictly Bazel-managed

# IDE / OS specific
.idea/
.vscode/
*.swp
*~
.DS_Store

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work
go.work.sum

# env file
.env

# Ignore Bazel build output directories and symlinks
MODULE.bazel.lock